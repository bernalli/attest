// Typecheck probe for verifier tests. This works around the production tsconfig.json
// covering only src/ while Vitest transpiles test/ without typechecking it.
{
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true, "rootDir": "." },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
